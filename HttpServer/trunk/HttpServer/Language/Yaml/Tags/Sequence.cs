using System;
using System.Collections.Generic;
using System.Text;

namespace HttpServer.Language.Yaml.Tags
{
    /// <summary>
    /// The content of a sequence  node is an ordered series of zero or more nodes. 
    /// In particular, a sequence may contain the same node more than once. It could even contain itself (directly or indirectly).
    /// </summary>
    class Sequence
    {
    }
}
