using System;
using System.Collections.Generic;
using System.Text;

namespace HttpServer.Language.Yaml.Tags
{
    /// <summary>
    /// 
    /// </summary>
    /// <remarks>
    /// The content of a mapping  node is an unordered set of key: value node pairs, 
    /// with the restriction that each of the keys is unique. YAML places no further restrictions on the nodes. 
    /// In particular, keys may be arbitrary nodes, the same node may be used as the value of several key: value pairs, 
    /// and a mapping could even contain itself as a key or a value (directly or indirectly).
    /// </remarks>
    class Mapping
    {
    }
}
