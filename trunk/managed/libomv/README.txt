README

These are libomv versions that had patches especially applied for OpenSimulator.

Recent libomv code has been used as obtained directly from https://github.com/openmetaversefoundation/libopenmetaverse instead.

Please see the OpenSimulator source control logs for the required version.

For example, using the command

git log bin/OpenMetaverse.dll
